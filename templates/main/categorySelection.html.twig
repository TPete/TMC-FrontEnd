<div class="row">
    {% set icons = {
        'movies': 'fa-film',
        'shows': 'fa-television'
    } %}
    {% for type, entries in categories %}
        {% for link, label in entries %}
            <div class="col-md-4">
                <div class="card card-block">
                    <h1 class="card-title">
                        <i class="fa {{ icons[type] }}"></i>
                        {{ label }}
                    </h1>
                    <p class="card-text">

                    </p>
                    <a class="btn btn-primary pull-xs-right" href="{{ link }}">
                        weiter
                        <i class="fa fa-chevron-right"></i>
                    </a>
                </div>
            </div>
        {% endfor %}
    {% endfor %}
</div>
